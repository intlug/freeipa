---
- name: Provision FreeIPA demo VMs
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    overwrite: false
    cidata_dir: /tmp/freeipa-cidata

  tasks:

    - name: Check base image exists
      ansible.builtin.stat:
        path: "{{ base_image }}"
      register: base_stat

    - name: Fail if base image not found
      ansible.builtin.fail:
        msg: "Base image not found: {{ base_image }}"
      when: not base_stat.stat.exists

    - name: Ensure base image is read-only
      ansible.builtin.file:
        path: "{{ base_image }}"
        mode: "a-w"

    - name: List currently defined VMs
      community.libvirt.virt:
        command: list_vms
        uri: "{{ libvirt_uri }}"
      register: defined_vms

    - name: Check for existing overlay disk images
      ansible.builtin.stat:
        path: "{{ storage_dir }}/{{ vm.name }}.qcow2"
      loop: "{{ vms }}"
      loop_control:
        loop_var: vm
        label: "{{ vm.name }}"
      register: disk_stat

    - name: Abort if VMs or disks already exist without overwrite flag
      ansible.builtin.fail:
        msg: >-
          '{{ item.vm.name }}' already exists
          (VM defined: {{ item.vm.name in defined_vms.list_vms }},
          disk: {{ item.stat.exists }}).
          Re-run with -e overwrite=yes to overwrite.
      when:
        - not (overwrite | bool)
        - (item.vm.name in defined_vms.list_vms) or item.stat.exists
      loop: "{{ disk_stat.results }}"
      loop_control:
        label: "{{ item.vm.name }}"

    - name: Destroy running VMs
      community.libvirt.virt:
        name: "{{ vm.name }}"
        state: destroyed
        uri: "{{ libvirt_uri }}"
      loop: "{{ vms }}"
      loop_control:
        loop_var: vm
        label: "{{ vm.name }}"
      when:
        - overwrite | bool
        - vm.name in defined_vms.list_vms
      ignore_errors: true

    - name: Undefine existing VMs
      community.libvirt.virt:
        name: "{{ vm.name }}"
        command: undefine
        uri: "{{ libvirt_uri }}"
      loop: "{{ vms }}"
      loop_control:
        loop_var: vm
        label: "{{ vm.name }}"
      when:
        - overwrite | bool
        - vm.name in defined_vms.list_vms
      ignore_errors: true

    - name: Remove existing overlay disk images
      ansible.builtin.file:
        path: "{{ storage_dir }}/{{ vm.name }}.qcow2"
        state: absent
      loop: "{{ vms }}"
      loop_control:
        loop_var: vm
        label: "{{ vm.name }}"
      when: overwrite | bool

    - name: Remove existing cloud-init ISOs
      ansible.builtin.file:
        path: "{{ storage_dir }}/{{ vm.name }}-cidata.iso"
        state: absent
      loop: "{{ vms }}"
      loop_control:
        loop_var: vm
        label: "{{ vm.name }}"
      when: overwrite | bool

    - name: Register MAC to IP hostname mappings in libvirt network
      ansible.builtin.shell: >-
        virsh --connect {{ libvirt_uri }} net-update {{ libvirt_network }}
        add ip-dhcp-host
        "<host mac='{{ vm.mac }}' name='{{ vm.fqdn }}' ip='{{ vm.ip }}'/>"
        --live --config
      loop: "{{ vms }}"
      loop_control:
        loop_var: vm
        label: "{{ vm.name }}"
      register: net_update
      changed_when: net_update.rc == 0
      failed_when:
        - net_update.rc != 0
        - "'existing dhcp host entry' not in net_update.stderr"

    - name: Provision each VM
      ansible.builtin.include_tasks: tasks/provision_vm.yml
      loop: "{{ vms }}"
      loop_control:
        loop_var: vm
        label: "{{ vm.name }}"

    - name: Wait for SSH on all VMs
      ansible.builtin.wait_for:
        host: "{{ vm.ip }}"
        port: 22
        timeout: 300
        delay: 15
        state: started
      loop: "{{ vms }}"
      loop_control:
        loop_var: vm
        label: "{{ vm.name }}"


- name: Verify connectivity and install FreeIPA packages
  hosts: freeipa
  gather_facts: false
  become: true
  tags: install

  tasks:

    - name: Verify clients can reach the server
      ansible.builtin.command: >-
        ping -c 2 {{ hostvars['ipaserver']['ansible_host'] }}
      when: inventory_hostname in groups['ipa_clients']
      changed_when: false

    - name: Create .ssh directory for demo user
      ansible.builtin.file:
        path: /home/demo/.ssh
        state: directory
        owner: demo
        group: demo
        mode: "0700"

    - name: Copy demo user private key
      ansible.builtin.copy:
        src: "{{ playbook_dir }}/keys/demokey"
        dest: /home/demo/.ssh/demokey
        owner: demo
        group: demo
        mode: "0600"

    - name: Write demo user SSH config
      ansible.builtin.copy:
        content: |
          Host *.{{ domain }}
            IdentityFile ~/.ssh/demokey
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
        dest: /home/demo/.ssh/config
        owner: demo
        group: demo
        mode: "0600"

    - name: Update all packages
      ansible.builtin.dnf:
        name: "*"
        state: latest

    - name: Install common DNF groups
      ansible.builtin.dnf:
        name: "{{ common_dnf_groups }}"
        state: present

    - name: Install FreeIPA packages and groups
      ansible.builtin.dnf:
        name: "{{ freeipa_packages }}"
        state: present

    - name: Enable and start firewalld
      ansible.builtin.service:
        name: firewalld
        state: started
        enabled: true
      when: inventory_hostname in groups['ipa_server']

    - name: Confirm FreeIPA server packages installed
      ansible.builtin.command: rpm -q freeipa-server freeipa-server-dns
      when: inventory_hostname in groups['ipa_server']
      changed_when: false

    - name: Confirm FreeIPA client package installed
      ansible.builtin.command: rpm -q freeipa-client
      when: inventory_hostname in groups['ipa_clients']
      changed_when: false
